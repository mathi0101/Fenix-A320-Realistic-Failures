<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Brushes.xaml"/>
        <ResourceDictionary Source="Typography.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Slider -->
    <Style x:Key="DarkSliderStyle" TargetType="Slider">
        <Setter Property="Foreground" Value="{StaticResource Brush.Accent.Blue.Primary}"/>
        <Setter Property="Background" Value="{StaticResource Brush.Border.Default}"/>
    </Style>

    <!-- Log entry -->
    <Style x:Key="LogEntryStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Monospace}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize.SM}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}"/>
        <Setter Property="Padding" Value="0,2"/>
    </Style>

</ResourceDictionary>